/**
 * ADAPTED STUB — OmniRoute's @/lib/events/eventBus (emit) is app infra not
 * present in OryphemRouter. No-op event emitter.
 */
export function emit() { return undefined; }
export default { emit };
