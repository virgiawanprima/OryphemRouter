// ADAPTED STUB — deep app infra (compression/preservation.ts).
export function extractPreservedBlocks(text) {
  return { text, blocks: [] };
}
export function restorePreservedBlocks(text, _blocks) {
  return text;
}
