// ADAPTED STUB — OmniRoute `@/models#getHiddenModelsByProvider`. OryphemRouter
// has no model-compat overrides DB; empty map = nothing hidden (graceful).
export function getHiddenModelsByProvider() {
  return new Map();
}
export default { getHiddenModelsByProvider };
