// ADAPTED STUB — OmniRoute `@omniroute/open-sse/config/freeModelCatalog.ts`
// (FREE_MODEL_BUDGETS). OryphemRouter has no curated free-model budget catalog;
// empty list means no candidate passes STRICT_ZERO_COST / free-only filters
// (fail-safe, only active when the operator opts in).
export const FREE_MODEL_BUDGETS = [];
export default { FREE_MODEL_BUDGETS };
