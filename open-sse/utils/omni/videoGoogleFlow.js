// ADAPTED STUB — OmniRoute `open-sse/handlers/videoGeneration/googleFlow.ts`
// exports `resolveVideoCredentialProvider`, mapping a video provider to the
// credential provider used for auth. OryphemRouter resolves credentials via
// its own auth path; identity fallback keeps videoCombo functional.
export function resolveVideoCredentialProvider(provider) {
  return provider;
}
