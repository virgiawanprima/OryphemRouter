// ADAPTED STUB — OmniRoute `src/domain/prompts.ts#renderPrompt`. OryphemRouter
// has no prompt-rendering domain; pass-through (graceful).
export function renderPrompt(input) {
  return input;
}
export default { renderPrompt };
