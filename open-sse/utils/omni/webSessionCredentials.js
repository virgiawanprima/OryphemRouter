// ADAPTED STUB — ported from OmniRoute src/shared/providers/webSessionCredentials.ts
// Only `WEB_SESSION_CREDENTIAL_REQUIREMENTS` is needed (by keepaliveThreshold.js).
// Empty map = graceful fallback (no provider flagged as cookie-session based).
export const WEB_SESSION_CREDENTIAL_REQUIREMENTS = {};
